A simple machine learning project that classifies Iris flower species using K-Nearest Neighbors (KNN) and Logistic Regression.
The dataset is standardized and split into training and testing sets, and both models are compared using accuracy, classification reports, and confusion matrices.
